# railway.toml — n8n service config
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "n8n start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "n8n"

[services.variables]
PORT = "5678"
